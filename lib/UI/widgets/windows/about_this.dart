import 'package:flutter/cupertino.dart';

class AboutThisContainer extends StatelessWidget {
  const AboutThisContainer({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
